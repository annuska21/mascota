<h1>Listado tamaño</h1>
<%= link_to 'Nuevo tamaño', new_pet_size_path %>
<table class="table table-hover">
  <tr>
    <th>Tamaño</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pet_sizes.each do |pet_size| %>
  <tr>
    <td><%= pet_size.name %></td>
    <td><%= link_to 'Editar', edit_pet_size_path(pet_size) %></td>
    <td><%= link_to 'Borrar', pet_size, method: :delete, data: { confirm: 'Estás seguro?' } %></td>
  </tr>
<% end %>
</table>

<br />


