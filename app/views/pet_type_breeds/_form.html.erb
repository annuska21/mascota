<%= form_for(@pet_type_breed) do |f| %>
  <% if @pet_type_breed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pet_type_breed.errors.count, "error") %> prohibited this pet_type_breed from being saved:</h2>

      <ul>
      <% @pet_type_breed.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :raza %><br />
    <%= f.text_field :raza %>
  </div>
  <div class="field">
    <%= f.label :pet_type_id %><br />
    <%= f.number_field :pet_type_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
