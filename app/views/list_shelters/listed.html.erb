 <div class="enlaces">
  <%= link_to '<< Volver al listado', list_shelters_path %>
</div>

 <h1>Listado de mascotas de <%= @shelter.name %></h1>

<div class="row">
     <% @pets.each do |pet| %>
        <div class="col-lg-4 col-md-6 col-sm-6">
          <div class="item">
            <div class="img-peq">
           <%= image_tag(pet.picture_url, :size => '250x250', :alt => 'imagen') if pet.picture? %>       
             </div>           
             <div class="info">
              <h3><%= pet.name %> | <%= pet.pet_type.name %></h3>
                <p>Raza: <%= pet.pet_type_breed.name %></p>
                <p>Años: <%= pet.age %> Sexo: <%= pet.gender %></p>
                     
             </div>
            </div>
          </div>
      <% end %>
</div>
      <div class="apple_pagination">
      <%= will_paginate @pets, :container => false %>
      </div>

