<% @page_title = "Login" %>
<div class="login">
  <%= form_tag(:action => 'attempt_login') do %>
  <table>
    <tr>
      <td><%= label_tag(:usuario) %></td>
      <td><%= text_field_tag(:usuario) %></td>
    </tr>
    <tr>
      <td><%= label_tag(:password) %></td>
      <td><%= password_field_tag(:password) %></td>
    </tr>
      <td><%= submit_tag("Log In") %></td>
    </table>
  <% end %>
</div>