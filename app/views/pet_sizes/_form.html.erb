<%= form_for(@pet_size) do |f| %>
   <%= render :partial=>"layouts/errors_panel" , object: @pet_size %>

  <div class="field">
    <%= f.label :pet_size %><br />
    <%= f.text_field :pet_size %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
