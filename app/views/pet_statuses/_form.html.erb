<%= form_for(@pet_status) do |f| %>
  <% if @pet_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pet_status.errors.count, "error") %> prohibited this pet_status from being saved:</h2>

      <ul>
      <% @pet_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :estado %><br />
    <%= f.text_field :estado %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
